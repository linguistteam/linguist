import { StyleSheet } from 'react-native';

export const profileStyles = StyleSheet.create({
  profileContent: {
    backgroundColor: '#ffffff',
    // TODO: Create app wide container?
    paddingHorizontal: 20,
  },
  hireButton: {
    marginBottom: 10,
  },
});
